# Release packet — Witness Register 0.1.0 (2026-07-29)

This packet accompanies the public release of Witness Register v0.1.0.
Released: 2026-07-29. Repository: <https://github.com/docxology/witness_register>

## Gates (my runs, this tree)

- Full suite: **194 passed**, branch coverage 98.16%, misses in `battery.py`,
  `invariants.py`, `figures.py`, and `relations.py` (`fail_under=90`) — also
  measured green under a real Python 3.10.
- `ruff check` + `ruff format --check` over `src tests scripts`: both clean.
- Double figure build: byte-identical across all 7 artifacts (3 plates,
  SVG+PNG, registry).
- Engine render: full validation pass, zero undefined references.

## Artifacts

- `witness_register_combined_0.1.0_2026-07-29.pdf` — SHA-256
  `0d4bd245ea9bd7b2995d1feb1254ac0ebd03f00c920ad4890ad713ddbb40cf52`.
- `data/envelopes/` — eight real exported envelopes (four worked, four
  same-subject) with provenance README; the same-subject set met the first
  worked chain's return contract.

## Release note

0.1.0 is the whole first arc: the package born in answer to "The Space
Between the Lines", the adversarial hardening pass (four defects found and
fixed), the worked example over real exports, the formal core, three
live-derived plates, verified scholarship, and the same-subject
co-registration whose posture is held at 0 — not crowned. Tip anchoring is
prepared (`anchor_statement`) and the first anchor is stored in the anchors
repository at <https://github.com/docxology/anchors> (planned for concurrent
public release).
