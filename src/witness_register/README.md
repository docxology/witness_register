# witness_register

The Shared Witness Register: co-registration without aggregation.

## Quick reference

```python
from witness_register import (
    genesis_state,
    update_state,
    verify_chain,
    project,
    run_battery,
    intake_envelope,
)
```

For the full contract, read `AGENTS.md` in this directory.
