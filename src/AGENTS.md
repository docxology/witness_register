# Source folder

Business logic lives under `witness_register/`. Thin orchestrators go in
`scripts/`; tests in `tests/`; prose in `manuscript/`.

See `witness_register/AGENTS.md` for the package-level contract.
