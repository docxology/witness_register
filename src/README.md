# Source

The `witness_register` package — a shared register for line report envelopes
that co-registers without aggregation, ranking, or override.

```bash
uv run pytest tests/ --cov=src --cov-fail-under=90
```

See [AGENTS.md](AGENTS.md) for the working contract.
