# Tests

194 tests covering the state chain, projection invariants, battery, figure
determinism, and cross-module contracts. Run with:

```bash
uv run pytest tests/ --cov=src --cov-fail-under=90
```

See [AGENTS.md](AGENTS.md) for the working contract.
