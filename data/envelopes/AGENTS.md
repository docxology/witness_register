# Envelopes contract

Four real `line.report-envelope/1.0` records stored by value. Nothing here was
produced by importing a line package — each envelope was generated by running
that line's own public API in its own repository.

See [`README.md`](README.md) for provenance records and generation commands.
